
# PostgreSQL Database config
spring.datasource.url=jdbc:postgresql://localhost:5432/isowhiz
spring.datasource.username=postgres
spring.datasource.password=asdfghjkl
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Properties
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# (Optional) If you want to use a connection pool, you can configure it like this (using HikariCP as an example)
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

# (Optional) Additional PostgreSQL settings
# spring.datasource.initialization-mode=always
# spring.datasource.platform=postgres
# spring.datasource.continue-on-error=true

# (Optional) Logging SQL queries
spring.jpa.show-sql=true
logging.level.org.hibernate.type.descriptor.sql=trace

# (Optional) Security
# spring.security.user.name=akshay
# spring.security.user.password=asdfghjkl
# spring.security.user.roles=ADMIN

# (Optional) JWT Authentication
app.jwt-secret=VuaKJRFX4oPAResZf7JtzZH07TZtKQIxvt9r3bCQ5sLAXb4DB893vNikexeXWOti
app-jwt-expiration-milliseconds=604800000